<?xml version="1.0" encoding="UTF-8"?>
<flow>
    <chain name="chain1">
        THEN(a, b, c);
    </chain>

    <chain name="parityCheck">
        SWITCH(d).to(e, f);
    </chain>

    <chain name="forTest">
        THEN(
            ITERATOR(IteratorTest).DO(THEN(e,f)),
            e
        );
    </chain>
</flow>
